# Development override — exposes DB & Redis ports to host, disables Traefik
services:
  traefik:
    profiles: ["production"]  # Don't start Traefik in dev
  app:
    ports:
      - "3000:3000"
  postgres:
    ports:
      - "5432:5432"
  redis:
    ports:
      - "6379:6379"
