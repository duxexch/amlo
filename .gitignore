node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# ── Replit state ──
.local/
.replit
replit.nix

# ── IDE / Editor ──
.vscode/
.idea/

# ── Dev artifacts ──
MEMORY.md
lt_*.txt
معلومات

# ── Secrets ──
.env
.env.local
.env.production

# ── Signing keys (NEVER commit) ──
signing/
*.keystore
*.jks