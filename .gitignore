node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# ── Replit state ──
.local/
.replit
replit.nix

# ── IDE / Editor ──
.vscode/
.idea/

# ── Dev artifacts ──
MEMORY.md
WORK_MEMORY.md
lt_*.txt
load-test-results.txt
OPTIMIZATION-PLAN.md
OPTIMIZATION-REPORT.md
OPTIMIZATION-REPORT-V2.md
LOAD-TEST-REPORT.md
معلومات

# ── Secrets ──
.env
.env.local
.env.production

# ── Signing keys (NEVER commit) ──
signing/
*.keystore
*.jks

# ── Google Play / App Store packages (source) ──
Ablox-GooglePlay/
*.zip

# Allow download files for distribution
!client/public/download/*.apk
!client/public/download/*.aab