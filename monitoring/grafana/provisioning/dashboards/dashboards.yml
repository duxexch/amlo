apiVersion: 1

providers:
  - name: "AMLO Dashboards"
    orgId: 1
    folder: "AMLO"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
